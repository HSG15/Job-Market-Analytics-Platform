{
	"dag": {},
	"jobConfig": {
		"command": "glueetl",
		"description": "",
		"role": "arn:aws:iam::451075103928:role/JobMarketGlueRole",
		"scriptName": "hirebase_bronze_to_silver.py",
		"version": "5.0",
		"language": "python-3",
		"scriptLocation": "s3://aws-glue-assets-451075103928-us-east-1/scripts/",
		"temporaryDirectory": "s3://aws-glue-assets-451075103928-us-east-1/temporary/",
		"timeout": 480,
		"maxConcurrentRuns": 1,
		"workerType": "G.1X",
		"numberOfWorkers": "5",
		"maxRetries": 0,
		"metrics": "",
		"observabilityMetrics": "",
		"security": "none",
		"bookmark": "",
		"logging": "",
		"spark": false,
		"sparkConfiguration": "standard",
		"sparkPath": "",
		"serverEncryption": false,
		"glueHiveMetastore": true,
		"etlAutoScaling": false,
		"etlAutoTuning": false,
		"etlAutoTuningJobRules": "",
		"jobParameters": [],
		"tags": [],
		"connectionsList": [],
		"jobMode": "NOTEBOOK_MODE",
		"name": "hirebase_bronze_to_silver",
		"kernel": "glueetl",
		"pythonPath": null,
		"dependentPath": "",
		"referencedPath": "",
		"pythonShellPrebuiltLibraryOption": "analytics",
		"pythonVersion": ""
	},
	"hasBeenSaved": false,
	"script": "import sys\nfrom pyspark.sql.functions import *\nfrom pyspark.sql.types import *\nfrom pyspark.sql import SparkSession\nfrom awsglue.context import GlueContext\nfrom awsglue.job import Job\nfrom awsglue.utils import getResolvedOptions\nfrom pyspark.context import SparkContext\nfrom datetime import datetime\nsc = SparkContext()\nglueContext = GlueContext(sc)\nspark = glueContext.spark_session\njob = Job(glueContext)\njob.init(\"bronzeToSilverJob\", {})\n# read bronze file dynamically\ntoday = datetime.now()\nday = today.strftime(\"%d\")\nmonth = today.strftime(\"%m\")\nyear = today.strftime(\"%Y\")\n\nbronze_path = f\"s3://job-market-tracker-hsg/bronze/year={year}/month={month}/day={day}/\"\n\ndf = spark.read.option(\"multiline\", \"true\").json(bronze_path)\ndf_jobs = df.select(explode(col(\"jobs\")).alias(\"job\"))\n\nfrom pyspark.sql.functions import current_date\n\ndf_silver = df_jobs.select(\n    col(\"job._id\").alias(\"job_id\"),\n    col(\"job.company_name\"),\n    col(\"job.job_title\"),\n    col(\"job.job_type\"),\n    col(\"job.location_type\"),\n    to_date(col(\"job.date_posted\")).alias(\"date_posted\"),\n    \n    col(\"job.yoe_range.min\").alias(\"min_yoe\"),\n    col(\"job.yoe_range.max\").alias(\"max_yoe\"),\n    \n    col(\"job.locations\")[0][\"city\"].alias(\"location_city\"),\n    col(\"job.locations\")[0][\"region\"].alias(\"location_region\"),\n    col(\"job.locations\")[0][\"country\"].alias(\"location_country\"),\n    \n    col(\"job.technologies\"),\n    col(\"job.skills\"),\n    col(\"job.description\")\n)\ndf_silver = df_silver.dropDuplicates([\"job_id\"])\ndf_silver = df_silver.withColumn(\"year\", lit(year)) \\\n    .withColumn(\"month\", lit(month)) \\\n    .withColumn(\"day\", lit(day))\n#write to silver dynamically\nsilver_path = f\"s3://job-market-tracker-hsg/silver/\"\ndf_silver.write \\\n    .mode(\"append\") \\\n    .partitionBy(\"year\", \"month\", \"day\") \\\n    .parquet(silver_path)\n\njob.commit()",
	"notebook": {
		"metadata": {
			"kernelspec": {
				"name": "glue_pyspark",
				"display_name": "Glue PySpark",
				"language": "python"
			},
			"language_info": {
				"name": "Python_Glue_Session",
				"mimetype": "text/x-python",
				"codemirror_mode": {
					"name": "python",
					"version": 3
				},
				"pygments_lexer": "python3",
				"file_extension": ".py"
			}
		},
		"nbformat_minor": 4,
		"nbformat": 4,
		"cells": [
			{
				"cell_type": "code",
				"source": "import sys\nfrom pyspark.sql.functions import *\nfrom pyspark.sql.types import *\nfrom pyspark.sql import SparkSession\nfrom awsglue.context import GlueContext\nfrom awsglue.job import Job\nfrom awsglue.utils import getResolvedOptions\nfrom pyspark.context import SparkContext\nfrom datetime import datetime",
				"metadata": {
					"trusted": true,
					"tags": []
				},
				"execution_count": 6,
				"outputs": [
					{
						"name": "stdout",
						"text": "\n",
						"output_type": "stream"
					}
				]
			},
			{
				"cell_type": "code",
				"source": "sc = SparkContext()\nglueContext = GlueContext(sc)\nspark = glueContext.spark_session\njob = Job(glueContext)\njob.init(\"bronzeToSilverJob\", {})",
				"metadata": {
					"trusted": true,
					"tags": []
				},
				"execution_count": 4,
				"outputs": [
					{
						"name": "stdout",
						"text": "\n",
						"output_type": "stream"
					}
				]
			},
			{
				"cell_type": "code",
				"source": "# read bronze file dynamically\ntoday = datetime.now()\nday = today.strftime(\"%d\")\nmonth = today.strftime(\"%m\")\nyear = today.strftime(\"%Y\")\n\nbronze_path = f\"s3://job-market-tracker-hsg/bronze/year={year}/month={month}/day={day}/\"\n\ndf = spark.read.option(\"multiline\", \"true\").json(bronze_path)",
				"metadata": {
					"trusted": true,
					"tags": []
				},
				"execution_count": 12,
				"outputs": [
					{
						"name": "stdout",
						"text": "\n",
						"output_type": "stream"
					}
				]
			},
			{
				"cell_type": "code",
				"source": "df_jobs = df.select(explode(col(\"jobs\")).alias(\"job\"))\n",
				"metadata": {
					"trusted": true,
					"tags": []
				},
				"execution_count": 13,
				"outputs": [
					{
						"name": "stdout",
						"text": "\n",
						"output_type": "stream"
					}
				]
			},
			{
				"cell_type": "code",
				"source": "from pyspark.sql.functions import current_date\n\ndf_silver = df_jobs.select(\n    col(\"job._id\").alias(\"job_id\"),\n    col(\"job.company_name\"),\n    col(\"job.job_title\"),\n    col(\"job.job_type\"),\n    col(\"job.location_type\"),\n    to_date(col(\"job.date_posted\")).alias(\"date_posted\"),\n    \n    col(\"job.yoe_range.min\").alias(\"min_yoe\"),\n    col(\"job.yoe_range.max\").alias(\"max_yoe\"),\n    \n    col(\"job.locations\")[0][\"city\"].alias(\"location_city\"),\n    col(\"job.locations\")[0][\"region\"].alias(\"location_region\"),\n    col(\"job.locations\")[0][\"country\"].alias(\"location_country\"),\n    \n    col(\"job.technologies\"),\n    col(\"job.skills\"),\n    col(\"job.description\")\n)",
				"metadata": {
					"trusted": true,
					"tags": []
				},
				"execution_count": 14,
				"outputs": [
					{
						"name": "stdout",
						"text": "\n",
						"output_type": "stream"
					}
				]
			},
			{
				"cell_type": "code",
				"source": "df_silver = df_silver.dropDuplicates([\"job_id\"])\ndf_silver = df_silver.withColumn(\"year\", lit(year)) \\\n    .withColumn(\"month\", lit(month)) \\\n    .withColumn(\"day\", lit(day))",
				"metadata": {
					"trusted": true,
					"tags": []
				},
				"execution_count": 17,
				"outputs": [
					{
						"name": "stdout",
						"text": "\n",
						"output_type": "stream"
					}
				]
			},
			{
				"cell_type": "code",
				"source": "#write to silver dynamically\nsilver_path = f\"s3://job-market-tracker-hsg/silver/\"\ndf_silver.write \\\n    .mode(\"append\") \\\n    .partitionBy(\"year\", \"month\", \"day\") \\\n    .parquet(silver_path)\n\njob.commit()",
				"metadata": {
					"trusted": true,
					"tags": []
				},
				"execution_count": 18,
				"outputs": [
					{
						"name": "stdout",
						"text": "\n",
						"output_type": "stream"
					}
				]
			},
			{
				"cell_type": "code",
				"source": "",
				"metadata": {},
				"execution_count": null,
				"outputs": []
			}
		]
	}
}